org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

author=NadienDev
group=nadiendev.ultimatemodadditions
modid=ultimatemodadditions
mod_name=UltimateModAdditions
mod_named=Ultimate Mod Additions
description=Coliseum dimension with difficulty-scaled mobs, JSON and KubeJS driven loot, Argentine coins and loot bags, and a Reskillable-style skill system.
license=MIT
mod_version=1.0.0
tag_lanzador=NeoForge

mcversion=1.21.1
neoversion=21.1.240
loader_version=[4,)

neogradle.subsystems.parchment.minecraftVersion=1.21.1
neogradle.subsystems.parchment.mappingsVersion=2024.11.17

jei_version=19.27.0.340

# Optional compile-time integrations. Turn any of these off if its maven is unreachable;
# the matching compat module is simply left out of the build.
compat_kubejs=true

kubejs_version=2101.7.2-build.372
rhino_version=2101.2.7-build.81

# Mods loaded only inside the dev runs, so KubeJS scripts can be tested
# without assembling a modpack. Set dev_runtime_mods to false if the maven is down.
dev_runtime_mods=true
